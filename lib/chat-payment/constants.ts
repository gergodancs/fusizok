/** Új fusizók alapértelmezett ingyenes chat-válasz kreditje (DB default is 1). */
export const DEFAULT_FREE_CREDITS = 1;

/** Fusizó bemutatkozás maximális hossza. */
export const CRAFTSMAN_BIO_MAX_LENGTH = 2000;
